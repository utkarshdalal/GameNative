package app.gamenative.theme.model

/**
 * Theme engine constants and versioning.
 */
object ThemeEngine {
    /**
     * The current major version of the Theme Engine supported by the app.
     * Themes must declare a matching `engineVersion` in their manifest to be considered compatible.
     */
    const val ENGINE_MAJOR: Int = 1
}
