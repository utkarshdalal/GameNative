<layouts>
  <!-- Main center-locked horizontal carousel -->
  <carousel id="mainCarousel"
            direction="right"
            itemWidth="720"
            itemHeight="320"
              itemSpacing="@{vars.itemSpacing}"
            selectionMode="stationary"
            centeredSelection="true"
            pageSize="5"
              itemCard="bigHeroCard" />
</layouts>
