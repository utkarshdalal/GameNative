<layouts>
    <!-- Optional supporting grid below the main row (moving selection) -->
    <grid id="supportGrid"
          columns="6"
          cellWidth="300"
          cellHeight="450"
          hSpacing="12"
          vSpacing="12"
          itemCard="capsuleSmall"
          selectionMode="moving" />
</layouts>
